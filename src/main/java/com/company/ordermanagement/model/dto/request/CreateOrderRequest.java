package com.company.ordermanagement.model.dto.request;

// Activity 1 — implement this DTO
// Requirements:
//   - customerId (UUID, required)
//   - lineItems (non-empty list of LineItemRequest)
//     - productId (UUID), productSku, productName, quantity (1-1000), unitPrice (> 0, BigDecimal)
public class CreateOrderRequest {
}
